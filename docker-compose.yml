version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - deployforge-network

  # Services will be added here as they are scaffolded
  # api:
  #   build: ./apps/api
  # ...

networks:
  deployforge-network:
    driver: bridge
